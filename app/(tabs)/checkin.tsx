import { View, Text } from 'react-native';

export default function CheckIn() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Check-in coming soon</Text>
        </View>
    );
}